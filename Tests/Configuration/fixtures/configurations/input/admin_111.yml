# TEST
# disable the same action globally and in the entity

# CONFIGURATION
easy_admin:
    disabled_actions: ['action1']
    entities:
        Category:
            class: AppTestBundle\Entity\UnitTests\Category
            disabled_actions: ['action1']
