# TEST
# the configuration of the backend doesn't define any option to override
# templates, but in the filesystem there are some files that override some
# default templates for all entities and other templates that override them only
# for the AnotherTestEntity

# CONFIGURATION
easy_admin:
    entities:
        Category:
            class: AppTestBundle\Entity\UnitTests\Category
        Product:
            class: AppTestBundle\Entity\UnitTests\Product
