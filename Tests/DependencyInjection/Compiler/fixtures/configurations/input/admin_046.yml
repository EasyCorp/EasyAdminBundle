# TEST
# global backend actions remove one default action for the 'edit' view

# CONFIGURATION
easy_admin:
    edit:
        actions:
            ['-delete']
    entities:
        TestEntity:
            class: AppBundle\Entity\TestEntity
