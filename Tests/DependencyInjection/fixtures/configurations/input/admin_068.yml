# TEST
# entity actions remove one default action for the 'list' view

# CONFIGURATION
easy_admin:
    entities:
        TestEntity:
            class: AppBundle\Entity\TestEntity
            list:
                actions:
                    ['-search']
