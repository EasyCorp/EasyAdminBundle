# TEST
# entity actions remove one default action for the 'edit' view

# CONFIGURATION
easy_admin:
    entities:
        TestEntity:
            class: AppBundle\Entity\TestEntity
            edit:
                actions:
                    ['-delete']
