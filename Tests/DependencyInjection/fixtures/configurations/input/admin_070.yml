# TEST
# entity actions remove one default action for the 'show' view

# CONFIGURATION
easy_admin:
    entities:
        TestEntity:
            class: AppBundle\Entity\TestEntity
            show:
                actions:
                    ['-edit']
