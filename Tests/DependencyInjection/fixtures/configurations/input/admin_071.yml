# TEST
# entity actions remove one default action for the 'new' view

# CONFIGURATION
easy_admin:
    entities:
        TestEntity:
            class: AppBundle\Entity\TestEntity
            new:
                actions:
                    ['-list']
