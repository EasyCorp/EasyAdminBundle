# TEST
# 'new' option overrides 'form' values

# CONFIGURATION
easy_admin:
    entities:
        TestEntity:
            class: AppBundle\Entity\TestEntity
            form:
                fields: ['field1', 'field2']
                custom_option: 'custom value'
            new:
                fields: ['field3', 'field4']
