{% if field_options.is_localized %}
    {{ value|localizednumber(field_options.format) }}
{% elseif field_options.format %}
    {{ field_options.format|format(value) }}
{% else %}
    {{ value|number_format(2) }}
{% endif %}
