# TEST
# global backend actions remove one default action for the 'list' view
# using the extended action configuration

# CONFIGURATION
easy_admin:
    list:
        actions:
            - { name: '-search' }
    entities:
        Category:
            class: AppTestBundle\Entity\UnitTests\Category
