# TEST
# if the 'form_theme' option is null, the default form theme is used

# CONFIGURATION
easy_admin:
    design:
        form_theme: ~
    entities:
        - AppTestBundle\Entity\UnitTests\Category
