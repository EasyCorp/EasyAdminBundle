# TEST
# the '@EasyAdmin/form/bootstrap_4.html.twig' shortcut of 'form_theme' option is correctly transformed into
# the full form theme path

# CONFIGURATION
easy_admin:
    design:
        form_theme: '@EasyAdmin/form/bootstrap_4.html.twig'
    entities:
        - AppTestBundle\Entity\UnitTests\Category
