# TEST
# the '@EasyAdmin/form/bootstrap_3_layout.html.twig' shortcut of 'form_theme' option is correctly transformed into
# the full form theme path

# CONFIGURATION
easy_admin:
    design:
        form_theme: '@EasyAdmin/form/bootstrap_3_layout.html.twig'
    entities:
        - AppTestBundle\Entity\UnitTests\Category
