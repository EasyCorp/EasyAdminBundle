# TEST
# When using an array of form themes, the 'horizontal' and 'vertical' shortcuts
# must keep being automatically transformed into their full form theme paths

# CONFIGURATION
easy_admin:
    design:
        form_theme: ['horizontal', 'vertical']
