# TEST
# the color scheme can only be 'dark' ('light' was deprecated in 1.x version)

# EXCEPTION
expected_exception:
    class: \Symfony\Component\Config\Definition\Exception\InvalidConfigurationException
    message_string: 'The value "light" is not allowed for path "easy_admin.design.color_scheme". Permissible values: "dark"'

# CONFIGURATION
easy_admin:
    design:
        color_scheme: 'light'
    entities:
        TestEntity: AppTestBundle\Entity\UnitTests\Category
