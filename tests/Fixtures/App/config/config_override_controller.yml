imports:
    - { resource: config.yml }

framework:
    # This file overrides the EasyAdmin controller
    router: { resource: "%kernel.project_dir%/config/routing_override_controller.yml", utf8: true }
